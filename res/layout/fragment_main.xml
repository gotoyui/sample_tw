<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/LinearLayout1"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" >

        <android.support.v4.widget.SwipeRefreshLayout
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:id="@+id/refresh"
            android:layout_width="match_parent"
            android:layout_height="match_parent" >

            <ListView
                android:id="@+id/listview"
                android:layout_width="match_parent"
                android:layout_height="match_parent" >
            </ListView>
        </android.support.v4.widget.SwipeRefreshLayout>
    </LinearLayout>

    <Button
        android:id="@+id/tweet_before"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@+string/tw_button_before" />

    <!-- <LinearLayout -->
    <!-- android:layout_width="match_parent" -->
    <!-- android:layout_height="wrap_content" > -->


    <!-- <Button -->
    <!-- android:id="@+id/operate_timeline" -->
    <!-- android:layout_width="wrap_content" -->
    <!-- android:layout_height="30dp" -->
    <!-- android:layout_gravity="bottom" -->
    <!-- android:layout_weight="1" -->
    <!-- android:background="#c0c0c0" -->
    <!-- android:text="@string/operate_timeline" -->
    <!-- android:textSize="12sp" -->
    <!-- tools:ignore="ButtonStyle" /> -->


    <!-- <Button -->
    <!-- android:id="@+id/operate_mention" -->
    <!-- android:layout_width="wrap_content" -->
    <!-- android:layout_height="30dp" -->
    <!-- android:layout_gravity="bottom" -->
    <!-- android:layout_weight="1" -->
    <!-- android:background="#c0c0c0" -->
    <!-- android:text="@string/operate_mention" -->
    <!-- android:textSize="12sp" -->
    <!-- tools:ignore="ButtonStyle" /> -->


    <!-- <Button -->
    <!-- android:id="@+id/operate_favorite" -->
    <!-- android:layout_width="wrap_content" -->
    <!-- android:layout_height="30dp" -->
    <!-- android:layout_gravity="bottom" -->
    <!-- android:layout_weight="1" -->
    <!-- android:background="#c0c0c0" -->
    <!-- android:text="@string/operate_favorite" -->
    <!-- android:textSize="12sp" -->
    <!-- tools:ignore="ButtonStyle" /> -->


    <!-- <Button -->
    <!-- android:id="@+id/operate_me" -->
    <!-- android:layout_width="wrap_content" -->
    <!-- android:layout_height="30dp" -->
    <!-- android:layout_gravity="bottom" -->
    <!-- android:layout_weight="1" -->
    <!-- android:background="#c0c0c0" -->
    <!-- android:text="@string/operate_me" -->
    <!-- android:textSize="12sp" -->
    <!-- tools:ignore="ButtonStyle" /> -->
    <!-- </LinearLayout> -->

</LinearLayout>